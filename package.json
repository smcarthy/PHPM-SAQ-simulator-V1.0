{
  "name": "phpm-saq-simulator",
  "private": true,
  "version": "1.0.0",
  "description": "Deterministic smoke tests for PHPM SAQ simulator",
  "scripts": {
    "smoke:test": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
